---
import type { PageListProps } from "../models/PageList";

interface Props extends PageListProps {}

const { pages } = Astro.props;
---

<ul>
  {
    pages.map((page) => (
      <li>
        <a href={page.url}>{page.title}</a>
      </li>
    ))
  }
</ul>
